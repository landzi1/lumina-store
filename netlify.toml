[build]
  command = "npm run build"
  # Opcjonalnie: ustawienie katalogu publikacji, choć plugin sam to robi
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"
